#include<stdio.h>
int main()
{
	int n,i,sum=0,sign=1;
	printf("enter the value of n:");
	sscanf("%d",&n);
	for(i=0;i<=n;i++)
	{
		if(sign==1)
		{
	        sum+=i;
		}
		else
		{
			sum-=i;
		}
	    sign = -sign 	
	}
	printf("sum of the given series is %d:",sum);
	return 0;
	
}
